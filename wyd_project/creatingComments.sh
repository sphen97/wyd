#!/bin/bash
echo "creating comments"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=2), event=Event.objects.all().get(id=1), rating=2, text='Hello')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=7), event=Event.objects.all().get(id=3), rating=5, text='Happy Thanksgiving!')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=5), event=Event.objects.all().get(id=6), rating=5, text='Merry Christmas!')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=4), event=Event.objects.all().get(id=6), rating=5, text='Merry Christmas!')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=3), event=Event.objects.all().get(id=6), rating=5, text='Merry Christmas!')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=4), event=Event.objects.all().get(id=6), rating=5, text='Merry Christmas!')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=2), event=Event.objects.all().get(id=21), rating=5, text='Luke I Am Your Father Wasnt Even In The Original Movie')"
python3 manage.py shell -c "from event.models import (Event,Comment); from django.contrib.auth.models import User; Comment.objects.create(author=User.objects.all().get(id=2), event=Event.objects.all().get(id=4), rating=5, text='I want a hippopotamus for Christmas Only a hippopotamus will do I dont want a doll, no dinkey tinker toys I want a hippopotamus to play with and enjoy I want a hippopotamus for Christmas I dont think Santa Claus will mind, do you? He wont have to use a dirty chimney flue Just bring him through the front door Thats the easy thing to do I can see me now on Christmas morning Creeping down the stairs Oh what joy, what surprise When I open up my eyes To see a hippo hero standing there I want a hippopotamus for Christmas Only a hippopotamus will do No crocodiles, no rhinosaurus I only likes hippopotamuses And hippopotamuses like me, too Mom says a hippo would eat me up, but then Teacher says a hippo is a vegetarian Theres lots of room for him in our two-car garage Id feed him there and wash him there and give him his massage.')"
